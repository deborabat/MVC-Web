<!doctype html>
<html lang="pt-BR">
  <%- include('../../partials/head', { title: 'Palavras-chave' }) %>
  <body>
    <%- include('../../partials/navbar') %>
    <div class="container py-4">
      <div class="d-flex justify-content-between align-items-center mb-3">
        <h3>Palavras‑chave</h3>
        <a class="btn btn-sm btn-success" href="/admin/keywords/new">Nova Palavra‑chave</a>
      </div>

      <table class="table table-sm">
        <thead><tr><th>ID</th><th>Nome</th><th></th></tr></thead>
        <tbody>
          <% keywords.forEach(k => { %>
            <tr>
              <td><%= k.id %></td>
              <td><%= k.name %></td>
              <td>
                <a class="btn btn-sm btn-primary" href="/admin/keywords/<%= k.id %>/edit">Editar</a>
                <form method="post" action="/admin/keywords/<%= k.id %>/delete" style="display:inline" onsubmit="return confirm('Excluir?')">
                  <button class="btn btn-sm btn-danger">Excluir</button>
                </form>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>